# Test Runner

Run the tests and generate JSON outputs with:

bash run_test.sh

This will create:
- raw_results.json (pytest JSON report)
- output.json (summary)
- report.json (static analysis report of detected issues)
